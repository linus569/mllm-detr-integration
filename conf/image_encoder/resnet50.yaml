name: resnet50
model_path: facebook/detr-resnet-50
image_size: [384, 384] # normally min_size 800, max_size 1333
mean: [0.485, 0.456, 0.406]  # ImageNet normalization
std: [0.229, 0.224, 0.225]   # ImageNet normalization
num_image_tokens: 144 # with 384px - 144, with 800px - 625 (downsample by factor 32, into X*X featuremap)
interpolation: 2 #bicubic
#max_tokens: 6400 need to set this as more tokens are used